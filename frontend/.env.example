# API Backend URL
VITE_API_URL=http://localhost:8000

# Supabase Auth (opcional até a migração do login Google)
# Use a chave pública/publishable do projeto. Nunca use a service_role no frontend.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
